# Node modules (generated by npm install)
node_modules/

# npm logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Lock files (optional - can commit if you want reproducible builds)
package-lock.json

# Keep vendored assets (these are committed)
!open-props/
!*.css
!*.js
!viewer.js
