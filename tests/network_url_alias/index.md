---
title = "URL Alias Test Network"
id = "url-alias-test"
alias-template = "/en-US/docs/{{ slug }}"
alias-base-url = "https://developer.example.org"
---

A test network for URL alias resolution via `alias-template` and `url_aliases`.

```{network_children}
```
